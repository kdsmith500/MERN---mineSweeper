import React from 'react';
import './NewScoreButton.css';

const NewScoreButton = (props) => (
    <div className="NewScoreButton">
        NewScoreButton
    </div>
);

export default NewScoreButton;